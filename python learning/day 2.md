**String**
- string is a data type that stores a sequence of characters. eg."string", 'string',"""string"""
Escape sequence (\n)
    1. Basic operations
      - concatination(adds two strings in one )
    2. length of string 
        # len(str) 
**Indexing**
- position of character 
- starts from 0 to 1 ....
-  eg. str[3]

**Slicing**
- accessing parts of a string 
- syntax: str[starting_index : ending_index] # ending index is not included
- can be done with negative counting/index (-1,-2,-3)
**String functions**
1. str()
2. str.edswith("substring") -> return true if string endswith substring.
3. str.capitalize() -> capitalize once the first letter
4. str.replace("Old string","new string") -> replaces all old occurance 
5. str.find(word) ->return index of finded value
6. str.count(word) -> counts string or char 

there are many string functions .

**Conditional Statements**
1. if(condition), elif(condition1), .... elif(condition N), .... else, 